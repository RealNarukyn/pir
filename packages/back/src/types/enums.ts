/* eslint-disable no-unused-vars */

export enum TracksEnum {
    solo = 'padel solo',
    duo = 'padel duo',
    padbol = 'padbol',
}

export enum SkillEnum {
    noob = 'noob',
    amateur = 'amateur',
    pro = 'professional',
    any = 'any'
}
